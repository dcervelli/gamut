name: Release

on:
  push:
    tags: ['v*']

# No binaries are attached. Omarchy builds this package from source in an Arch
# container and signs the result itself, so what a release is for is to give
# the tag a stable, checksummable source tarball for the PKGBUILD to name.
jobs:
  release:
    name: Cut the GitHub release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: The tag agrees with Cargo.toml
        run: |
          version=$(sed -n 's/^version = "\(.*\)"$/\1/p' Cargo.toml | head -1)
          test "v$version" = "$GITHUB_REF_NAME" \
            || { echo "tag $GITHUB_REF_NAME but Cargo.toml is $version"; exit 1; }

      - name: Create the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes
